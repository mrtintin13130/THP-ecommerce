<div class="container" style="margin-top: 50px;">
  <div class="row">
    <div class="col">
    </div>
    <div class="col-4">
      <img class="card-img-top" src="<%= @item.image_url %>" alt="">
    </div>
    <div class="col-4">
      <h4><%= @item.title %></h4>
      <p><%= @item.description %></p>
      <p><%= @item.price %>€</p>
      <%= link_to 'ajouter <i class="fas fa-cart-plus"></i>'.html_safe, new_charge_path %>
    </div>
    <div class="col">
    </div>
  </div>
</div>
